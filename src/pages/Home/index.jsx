import {useState, useEffect} from "react";
import { Container, Typography } from "@mui/material";

const Home = () => {
    return(
        <Container sx={{ backgroundColor: '' }}>
            <Typography variant="h4" align="left">Pagina Principal :3</Typography>
        </Container>
    );
};

export default Home;